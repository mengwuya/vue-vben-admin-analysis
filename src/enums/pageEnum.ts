export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // 项目的基础根路由  必须存在 如果是后端路由则必须创建/dashboard路由
  BASE_HOME = '/dashboard',
  // error page path
  ERROR_PAGE = '/exception',
  // error log page path
  ERROR_LOG_PAGE = '/error-log/list',
}
